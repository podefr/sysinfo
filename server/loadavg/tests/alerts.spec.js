"use strict";

describe("Given initialized load average", _ => {
    describe("When getting load average data", function () {
       it("Then computes the average load average for the last 2 minutes", function () {

       });
    });
});